# Tasks — {{title}}

This file is for work that is **not** a contract test. Test-driven implementation is handled by the contract: run `devspec scaffold <slug>` and drive each failing test through red → green → refactor.

Use this file for:

- [ ] Update API docs / OpenAPI if the public surface changed
- [ ] Migration scripts, deployment notes, runbook updates
- [ ] Standards updates if conventions changed
- [ ] `devspec check` is clean
- [ ] `devspec coherence <slug>` is clean
